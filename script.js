
let output = 'car';
document.getElementById('big-screen').innerHTML = output;

function clear




function operate (a, o, b) {

    if (o === '+') {
        return a + b;
    }
    else if (o === '-') {
        return a - b;
    }
    else if (o === '*') {
        return a * b;
    }
    else if (o === '/') {
        return a / b;
    }
}